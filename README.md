# IntegrityChecker
A Python-based security tool that monitors files for unauthorized changes using cryptographic hashing. It creates a baseline of file hashes and compares them during subsequent scans to detect modified, deleted, or newly added files.
